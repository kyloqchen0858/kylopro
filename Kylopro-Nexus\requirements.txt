# Kylopro-Nexus 依赖清单
# 在激活 .venv 后运行: pip install -r requirements.txt

# === 核心框架 ===
nanobot-ai>=0.1.4       # nanobot Agent 内核（不修改源码，pip 引入）
python-dotenv>=1.0.0    # .env 密钥读取

# === 双核大脑 ===
openai>=1.0.0           # DeepSeek API（兼容 OpenAI 接口）
ollama>=0.3.0           # 本地 Ollama Python 客户端

# === 技能库 ===
httpx>=0.27.0           # Telegram 推送 / 轻量 HTTP 客户端
psutil>=5.9.0           # 系统资源监控（心跳检测）

# === 未来技能扩展（按需解除注释）===
# pyautogui>=0.9.54     # RPA 物理键鼠操作
# easyocr>=1.7.0        # OCR 视觉识别
# watchdog>=4.0.0       # 本地文件系统监控
